import 'package:flutter/material.dart';
class CloseDigi extends StatelessWidget {
  const CloseDigi({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      height: 300,
      width: 300,
      color: Colors.red,
    );
  }
}
